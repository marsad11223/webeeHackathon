import React from 'react';
import { View, StyleSheet } from 'react-native';

const Items: React.FC = ({ route }) => {

  console.log(route.params);

  return (
    <View >

    </View>
  );
};

const styles = StyleSheet.create({
});

export default Items;
